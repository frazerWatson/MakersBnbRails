<h1>Spaces#index</h1>
<% if @spaces.any? %>
  <% @spaces.each do |space| %>
    <p><%= space.name %></p>
  <% end %>
<% else %>
  <p>no spaces yet...</p>
<% end %>
<%= link_to 'post a space', new_space_path %>
